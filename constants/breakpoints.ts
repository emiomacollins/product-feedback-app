export enum Breakpoints {
	mobileUp = `(min-width: 426px)`,
	mobileDown = `(max-width: 425px)`,
	tabletUp = `(min-width: 801px)`,
	tabletDown = `(max-width: 800px)`,
	desktopUp = `(min-width: 1025px)`,
	desktopDown = `(max-width: 1024px)`,
}
