export enum Breakpoints {
	mobileUp = `(min-width: 425px)`,
	mobileDown = `(max-width: 425px)`,
	tabletUp = `(min-width: 768px)`,
	tabletDown = `(max-width: 768px)`,
	desktopUp = `(min-width: 1024px)`,
}
